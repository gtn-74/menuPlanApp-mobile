# Issue: CalendarScreen ボトムシートが上まで上がらない

## 概要
日付を選択した際に表示されるボトムシートが、スナップポイントを変更しても期待通りの高さまで上がってこない。

## 現象
- `snapPoints` を `["60%", "85%"]` から `["85%", "95%"]` に変更したが、ボトムシートの表示位置に変化が見られない
- 何かが高さを制限している可能性がある

## 対象ファイル
- `src/screens/CalendarScreen.tsx` (220行目付近)

## 調査ポイント
- ボトムシートの親コンテナやレイアウトが高さを制限していないか
- `@gorhom/bottom-sheet` v5 の設定で他に高さに影響するプロパティがないか
- カレンダーコンポーネントがオーバーレイを阻害していないか
- `BottomSheet` の `enableDynamicSizing` や `maxDynamicContentSize` などの設定確認

## ステータス
未解決
